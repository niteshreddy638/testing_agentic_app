{
  "name": "web-research-agent",
  "version": "1.0.0",
  "description": "Simple agentic app that crawls the web and writes a report using the Claude Agent SDK",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "test": "node --experimental-vm-modules node_modules/.bin/jest"
  },
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "^0.1.0"
  },
  "devDependencies": {
    "jest": "^30.4.2"
  }
}